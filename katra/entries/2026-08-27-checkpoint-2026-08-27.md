---
title: Checkpoint — 2026-08-27
date: "2026-08-27"
time: "14:14:38"
tags:
    - checkpoint
summary: Open loops captured before clearing context
---

### Checkpoint — 2026-08-27 14:14

**Memory awaiting resolution**
- `katra-v0-1-0-awaiting-tag.md` (pending)
- `katra-is-a-blog-routing-is-v0-2-0.md` (pending)
- `katra-v0-1-0-shipped.md` (pending)

**Where**
- draft: none
- branch: `owner/hooks-noop-without-katra` at `1b22ff6`

### What only this session knows

The derived block above is correct and nearly empty, because the tree is clean
and everything is committed and stamped. **Everything actually in flight is
outside the working tree**, which is the finding — see the entry that follows.

**Five open PRs, none merged. Merge order matters.**

| PR | branch | base | what |
|---|---|---|---|
| #7 | `owner/hooks-noop-without-katra` | main | hooks no-op without katra — the public-repo defect |
| #3 | `owner/threshold-sees-undocumented-work` | main | checkpoint measures risk; **plus a pre-existing porcelain path bug** |
| #6 | `owner/task-horizon-flag` | main | `--horizon` on `task new` |
| #4 | `owner/author-on-node-creation` | main | durable `author` + `author_role` |
| #5 | `owner/claim-derives-doing` | **#4** | claim + derived `doing` |

- **#4 before #5** — #5 is stacked and its diff will look wrong otherwise.
- **#3 and #5 both touch `checkpoint.go`** — whichever lands second needs a
  small conflict resolution.
- **#7 first if choosing one.** It is the only defect a stranger can hit, it is
  live on a public repo, and it is the project's front door.
- `owner/seam-design-held` has **no PR** and is superseded by #5. Delete it when
  #5 lands, unless the held-design record is wanted.

**A rename brainstem needs to know about.** `core.ActorToken()` no longer
exists. It split into `core.Author()` and `core.ClaimToken()`, reading
`KATRA_AUTHOR` / `KATRA_AUTHOR_ROLE` and `KATRA_CLAIM_TOKEN` respectively —
because one variable feeding both authorship and claims would have destroyed
authorship the moment a pane died. `KATRA_ACTOR` is gone entirely and is in no
release. brainstem was told and offered the chance to pick different names;
**if they have been faulting against `actor()`, that is why it is missing.**

**Held, deliberately, and not blocked on anything technical:** the dissent /
override field. Recorded as a decision with its reasoning. It attaches to a
ranking surface katra does not hold yet, so building it now would be a field
with nothing to disagree about.

**Not katra's to do, and I imported nothing:** the `on_deck` migration of nine
plan files (each project's own session), and the stem-side join.

**Craig's, unchanged and none of them blocking:** `HOMEBREW_TAP_GITHUB_TOKEN`
(releases still silently skip the tap), the `.goreleaser.yml` description
string, the head-injection extension point, `katra task cut`, and the
pre-commit gate's substring match that false-positives on any command
containing `git ` and `commit` — including `-c commit.gpgsign=false`.

Used deliberately rather than in an emergency, and it held less than half of what this session was carrying — for a reason that is structural rather than a bug. The derived block was correct and nearly empty: tree clean, everything committed and stamped, so nothing was at risk. Both the shipped version and the unmerged AtRisk fix produced identical output, which is the honest result. The checkpoint answers 'what is at risk in this working tree' accurately. This session's open loops were almost entirely somewhere else.

What it could not see, enumerated: five open PRs with a merge order and two sharing a file; six unmerged local branches; a rename another session is actively faulting against; and which of the held items are blocked on a person rather than on work. None of that lives in the working tree or the store.

One of those is cheaply fixable and one is not, and the line between them is the same one-directional dependency the seam ruling drew. Unmerged local branches are a git fact — git for-each-ref plus rev-list --count main..branch, no network, no dependency, and it would have named all six. Open pull requests are not: knowing them needs GitHub, and a katra that reaches for a forge to answer 'what is in flight' fails the same parity test that keeps it from reading stem. So the recommendation is narrow: report unmerged branches, never PRs, and say plainly that the forge is not consulted.

A second, smaller finding fell out: the checkpoint reported katra-v0-1-0-awaiting-tag.md as a pending memory obligation, and that file was deleted hours ago. The memory ledger holds generations for files that no longer exist, so a checkpoint can list an obligation nobody can act on — the reader cannot tell a real pending memory from a ghost. Same family as everything else today: a record that has come loose from the thing it describes.
